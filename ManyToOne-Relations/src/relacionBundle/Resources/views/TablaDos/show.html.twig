{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TablaDos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Sitiowebnombre</th>
                <td>{{ entity.sitiowebNombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ entity.direccion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tablados') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tablados_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
