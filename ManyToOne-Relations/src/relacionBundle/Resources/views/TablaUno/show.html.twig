{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TablaUno</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
            <tr>
                <th>Appaterno</th>
                <td>{{ entity.apPaterno }}</td>
            </tr>
            <tr>
                <th>Apmaterno</th>
                <td>{{ entity.apMaterno }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tablauno') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tablauno_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
